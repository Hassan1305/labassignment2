# labassignment2

Hello, 
welcome here bruh,

To run the code successfully do the following things
clone the repo
npm install
make sure you have XAMPP start the xampp
get to the XAMPP directory and htdocs folder place the three files of php in it, the files are currently in the api folder in the repo
get to phpmyadmin and create a new database of meter_reading
create a table of records with 4 columns

get to cmd now
do ipconfig

find your ip and place it in the links wherever fetch is used,
mainly there are two pages where fetch is being used,
those are ViewRecordsScreen, AddRecordScreen
